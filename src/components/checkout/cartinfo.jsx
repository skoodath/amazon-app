import { CartInfo } from "../../styles/checkout/cartinfo.style";

const CartInfoComponent = () => (
  <CartInfo>
    <p>Pay faster with amazon pay.</p>
  </CartInfo>
);

export default CartInfoComponent;
